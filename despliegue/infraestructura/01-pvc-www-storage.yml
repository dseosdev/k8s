apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: www-disk
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: default #managed-premium para premium
  resources:
    requests:
      storage: 1Gi # 1gb de almacenamiento